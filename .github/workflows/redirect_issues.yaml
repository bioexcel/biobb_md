name: Manual Issue Transer Action Test
on:
  issues:
    types:
      - labeled

jobs:
  test-action:
    runs-on: ubuntu-20.04

    steps:
      - name: Transfer Issue & Create Stub
        uses: lando/transfer-issue-action@v2
        with:
          token: ${{ secrets.github_token }}
          router: md:biobb_gromacs